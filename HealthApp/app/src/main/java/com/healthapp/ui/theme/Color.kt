package com.healthapp.ui.theme

import androidx.compose.ui.graphics.Color

// Sanari palette inspired by the provided gradients (blue → violet → pink) and light glassmorphism UI
val JaguarBlack = Color(0xFF0F1224) // Deep navy for dark text/surfaces
val JaguarCharcoal = Color(0xFFF4F6FF) // Soft misty surface for light cards
val JaguarDarkGray = Color(0xFFE6E9F8) // Subtle divider/border tone
val JaguarGold = Color(0xFF9C4FFF) // Primary violet
val JaguarGoldDim = Color(0xFF7B34D4) // Deeper violet for emphasis
val JaguarAccent = Color(0xFFFF6FB1) // Pink accent pulled from the gradient
val JaguarWhite = Color(0xFFFFFFFF)
val JaguarTextPrimary = Color(0xFF1C1D2D) // Rich ink for readability
val JaguarTextSecondary = Color(0xFF5A6375) // Muted slate for secondary text

// Gradients
val JaguarGradientStart = Color(0xFFF3F7FF) // Pale blue top
val JaguarGradientEnd = Color(0xFFFFF3FF) // Pale pink bottom

// Semantic Colors
val JaguarSuccess = Color(0xFF22C55E)
val JaguarError = Color(0xFFFF4D67)
val JaguarWarning = Color(0xFFFFB347)
